# 第一次培训作业报告


## 1. 开发环境、坐标系约定和重投影原理
- **开发环境**：WSL2 (Ubuntu)，G++ 编译器，CMake（apt 安装，非 Snap），Git。
- **坐标系约定**：外参为世界到相机变换 \(P_c = R \cdot P_w + t\)。三维坐标与平移量使用相同长度单位。
- **重投影原理**：
  1. 将世界坐标点 \(P_w\) 变换到相机坐标系 \(P_c = (X_c, Y_c, Z_c)\)。
  2. 检查深度 \(Z_c > 0\)，若 \(Z_c \le 0\) 抛出异常。
  3. 理想针孔模型投影：\(u = f_x \cdot \frac{X_c}{Z_c} + c_x\)，\(v = f_y \cdot \frac{Y_c}{Z_c} + c_y\)。
  4. 计算投影点与观测点之间的像素欧氏距离。

## 2. 项目结构、构建运行方法、测试结果及误差分析
- **项目结构**：
```

├── CMakeLists.txt
├── README.md
├── include/reprojection.hpp
└── src/
├── main.cpp
└── reprojection.cpp

```
- **构建运行方法**：
  ```bash
  cmake -S . -B build
  cmake --build build
  ./build/reprojection_app
```

· 测试结果：
  ```text
  --- 测试 1：正常深度 ---
  投影像素: (320, 240)
  观测像素: (320, 240)
  像素距离: 0
  --- 测试 2：非正深度 ---
  成功捕获到预期异常: Non-positive depth (Zc <= 0)
  ```
· 误差分析：本次测试使用理想数据（点在主光轴上），理论值与观测值完全一致，像素距离为 0，符合理想针孔模型预期。

3. 遇到的问题与解决办法

1. Git 作者身份未配置：提交时提示 Author identity unknown。
   · 解决：执行 git config user.name 和 git config user.email。
2. Git 命令拼写错误：输入了 git remove 报错。
   · 解决：正确命令是 git remote。
3. 远程仓库链接带尖括号：复制命令时把 < > 也复制进去了。
   · 解决：去掉尖括号，直接写 URL。
4. GitHub Token 问题：找不到 Personal Access Token，且密码不能直接用登录密码。
   · 解决：在 Settings -> Developer settings -> Personal access tokens (classic) 生成新 Token，勾选 repo 权限，并作为密码使用。

4. 代码仓库链接、PR链接和最终提交的commit hash

· 代码仓库链接：https://github.com/violet20070912/hahahahaha
· PR链接：https://github.com/violet20070912/hahahahaha/pull/1 （注：如果你刚才合并时 PR 编号不是 1，请直接去网页地址栏复制真实的链接替换这里）
· 最终提交的commit hash：6ef52dd
